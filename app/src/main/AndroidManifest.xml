<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.simpak">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/simpak_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Simpak">

        <activity android:name=".PilihKelasActivity" />
        <activity android:name=".ProposalActivity" />
        <activity android:name=".AddProposalActivity" />
        <activity android:name=".DetailProposalActivity" />
        <activity android:name=".DashboardActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />

        <activity
            android:name=".OnboardingActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>


</manifest>
